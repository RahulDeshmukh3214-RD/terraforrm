name: CI Workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building the project"
  test:
    runs-on: ubuntu-latest
    steps:
     - run: echo "Testing the project"    
